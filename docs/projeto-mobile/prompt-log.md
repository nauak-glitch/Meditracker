1.  **Criação dos Testes:**
    > "Crie um arquivo de teste unitário usando Jest e Testing Library para validar o schema Zod e a store Zustand do arquivo `medication.ts`."

2.  **Resolução de Erros de Versão (React 19):**
    > "Estou recebendo erro `ERESOLVE unable to resolve dependency tree` tentando instalar bibliotecas de teste no Expo SDK 54 com React 19. Como forçar a compatibilidade ou fazer downgrade seguro?"

3.  **Configuração do Jest no Expo:**
    > "O Jest está retornando `SyntaxError: Cannot use import statement outside a module`. Como configurar o `jest.config.js` e o `babel.config.js` para um projeto Expo TypeScript?"

4.  **Correção do Reanimated/Babel:**
    > "Erro `Cannot find module 'react-native-worklets/plugin'`. O app não abre. Como alinhar as versões do `react-native-reanimated` com o Expo SDK 54?"

5.  **Adaptação Web (Botão Excluir):**
    > "O `Alert.alert` não abre no navegador. Como criar uma função condicional que usa `window.confirm` na Web e `Alert.alert` no Mobile?"